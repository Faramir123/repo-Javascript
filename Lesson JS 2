<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <script>

        /* Задание 1

        
        c =++a 1+1=2 (по скольку стоит префикс, переменная а увеличивается на единицу и записывается в с равной 2)
        d = b++; в переменную в pfgbitncz tlbybwf? f pfntv b увеличится на единицу. Ответ будет равен 1
        c= 2 + ++a (2 + 2 + 1 То есть значение а на данной строке будерт равно 2 и увеличивается на единицу из-за префикса. Сумма равна 5)
        d=2 + b++ ( здесь увеличение b произойдёт после выполнения операции сложения. Получается 2+2 =4. Затем b=3
        в итоге а и b в конце кода увеличились два раза по единице. a=3 и b =3
        */

        // ЗАДАНИЕ 2
        // ф = 2*2 = 4, х = 1+ 4 = 5 Итог а=4 х=5

        // Задание 4
        function SumArg(par1, par2) {
            return c = a + b;
        }
        function RazArg(par1, par2) {
            return c = a - b;
        }
        function ProizArg(par1, par2) {
            return c = a * b;
        }
        function DelArg(par1, par2) {
            return c = a / b;
        }
        //задание 5
        //@operation задаем значение от 1 до 4. 1 "+", 2 "-", 3 "*", 4 "/"
        function Mathik(arg1, arg2, operation) {
            switch (operation) {
                case 1: // сумма "+"
                    return c = a + b;
                case 2: // разность "-"
                    return c = a - b;
                case 3: // умножение "*"
                    return c = a * b;
                case 4: // деление "/"
                    return c = a / b;

            }
        }

        //Задание 3

        let a = parseInt((Math.random() - 0.5) * 100);
        let b = parseInt((Math.random() - 0.5) * 100);
        let c = 0;
        if (a >= 0 && b >= 0) {
            c = a - b;
        } else if (a < 0 && b < 0) {
            c = a * b;
        } else if (a >= 0 && b < 0 || a < 0 && b >= 0) {
            c = a + b;
        }

        console.log(c);

        //Задание 5

        console.log(Mathik(a, b, 1));

        // ПРОДОЛЖЕНИЕ Задание 4
        console.log(SumArg(a, b));
        console.log(RazArg(a, b));
        console.log(ProizArg(a, b));
        console.log(DelArg(a, b)); */
    </script>
</body>

</html>